name: Sync Build and Release

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force build even if no upstream changes"
        required: false
        default: "false"
        type: string
      release_mode:
        description: "sync = version tracks upstream; feature = keep our local version.json (for our own feature releases)"
        required: false
        default: "sync"
        type: choice
        options:
          - sync
          - feature

concurrency:
  group: rterm-sync-build
  cancel-in-progress: false

env:
  NODE_VERSION: "20"
  UPSTREAM_REPO: "MrOrangeJJ/GyShell"
  UPSTREAM_BRANCH: "main"

permissions:
  contents: write

jobs:
  sync:
    name: Sync from upstream GyShell
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      new_version: ${{ steps.check.outputs.new_version }}
    steps:
      - name: Checkout RTerm
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: Check upstream changes
        id: check
        run: |
          CURRENT_VERSION=$(jq -r '.version' version.json 2>/dev/null || echo "0.0.0")
          echo "current_version=$CURRENT_VERSION"

          # In feature release mode we ship OUR local version (already bumped
          # in-tree). Skip the upstream comparison entirely and never
          # clobber version.json/package.json with upstream's number.
          if [ "${{ inputs.release_mode }}" = "feature" ]; then
            echo "Feature release mode: building and releasing v$CURRENT_VERSION as-is"
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "skip_upstream_merge=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # --- Trigger: new upstream COMMITS, not release version.
          # Version-based triggering breaks as soon as our local version runs
          # ahead of upstream's (e.g. our 1.6.3 vs their 1.6.0): their next few
          # releases would compare lower/equal and silently never sync.
          # Our history now contains upstream's history (one-time linking merge
          # with --allow-unrelated-histories), so ancestry answers "do we have
          # their HEAD yet?". An upstream SHA we've never fetched fails the
          # ancestry test, which correctly counts as new commits.
          UPSTREAM_SHA=$(git ls-remote "https://github.com/${{ env.UPSTREAM_REPO }}.git" "refs/heads/${{ env.UPSTREAM_BRANCH }}" | cut -f1)
          echo "upstream_sha=$UPSTREAM_SHA"
          if [ -z "$UPSTREAM_SHA" ]; then
            echo "Could not resolve upstream HEAD; skipping build"
            echo "should_build=false" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            exit 0
          fi

          if git merge-base --is-ancestor "$UPSTREAM_SHA" HEAD 2>/dev/null; then
            NEW_COMMITS=false
          else
            NEW_COMMITS=true
          fi
          echo "new_upstream_commits=$NEW_COMMITS"

          # --- Version: never regress. Take upstream's release number if it is
          # higher than ours; otherwise bump our patch so the new sync release
          # gets a fresh tag.
          UPSTREAM_LATEST=$(curl -sL "https://api.github.com/repos/${{ env.UPSTREAM_REPO }}/releases/latest" \
            | jq -r '.tag_name // "0.0.0"' | sed 's/^v//')
          echo "upstream_latest=$UPSTREAM_LATEST"

          CURRENT_PADDED=$(printf "%03d%03d%03d" $(echo "$CURRENT_VERSION" | tr '.' ' '))
          UPSTREAM_PADDED=$(printf "%03d%03d%03d" $(echo "$UPSTREAM_LATEST" | tr '.' ' '))

          if [ "$UPSTREAM_PADDED" -gt "$CURRENT_PADDED" ]; then
            SYNC_VERSION="$UPSTREAM_LATEST"
          else
            SYNC_VERSION="$(echo "$CURRENT_VERSION" | cut -d. -f1).$(echo "$CURRENT_VERSION" | cut -d. -f2).$(( $(echo "$CURRENT_VERSION" | cut -d. -f3) + 1 ))"
          fi

          if [ "$NEW_COMMITS" = "true" ]; then
            echo "New upstream commits detected → syncing as v$SYNC_VERSION"
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "new_version=$SYNC_VERSION" >> $GITHUB_OUTPUT
          elif [ "$UPSTREAM_PADDED" -gt "$CURRENT_PADDED" ]; then
            # Upstream tagged a newer release from commits we already merged:
            # adopt their number so we stay aligned.
            echo "Upstream released v$UPSTREAM_LATEST from already-merged commits → rebuilding"
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "new_version=$UPSTREAM_LATEST" >> $GITHUB_OUTPUT
          elif [ "${{ inputs.force }}" = "true" ]; then
            # Force rebuild with nothing new: re-release the CURRENT version
            # (release step is idempotent), never regress to upstream's number.
            echo "Force build requested (no upstream changes) → re-releasing v$CURRENT_VERSION"
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "skip_upstream_merge=true" >> $GITHUB_OUTPUT
          else
            echo "No new upstream commits (current: v$CURRENT_VERSION, upstream release: v$UPSTREAM_LATEST)"
            echo "should_build=false" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Merge upstream changes
        if: steps.check.outputs.should_build == 'true' && steps.check.outputs.skip_upstream_merge != 'true'
        run: |
          git config user.name "RTerm Sync Bot"
          git config user.email "rterm-bot@hyperspace.ng"
          git remote add upstream "https://github.com/${{ env.UPSTREAM_REPO }}.git"
          git fetch upstream ${{ env.UPSTREAM_BRANCH }}

          # Histories were linked by a one-time merge with
          # --allow-unrelated-histories, so this is a normal merge now.
          # -X ours: where upstream edited the same lines we customized, keep
          # ours; everything else from upstream flows in.
          git merge "upstream/${{ env.UPSTREAM_BRANCH }}" -X ours --no-edit --allow-unrelated-histories || {
            CONFLICTED=$(git diff --name-only --diff-filter=U)
            echo "::warning::Upstream sync conflicts resolved keep-ours in: $CONFLICTED"
            echo "$CONFLICTED" | while read -r file; do
              git checkout --ours "$file" 2>/dev/null || true
              git add "$file"
            done
            git commit --no-edit || git commit -m "chore: merge upstream GyShell (resolve conflicts keep-ours)"
          }

      - name: Apply rebrand
        if: steps.check.outputs.should_build == 'true'
        run: |
          # In feature release mode the rebrand + version clobber must NOT
          # run: our version.json/package.json already hold the correct local
          # version and our code changes are committed in-tree.
          if [ "${{ inputs.release_mode }}" = "feature" ]; then
            echo "Feature release mode: skipping rebrand + version clobber"
            exit 0
          fi
          chmod +x scripts/rebrand.sh 2>/dev/null || true
          if [ -f "scripts/rebrand.sh" ]; then
            bash scripts/rebrand.sh
          fi
          # Restore RTerm icons (overwritten by upstream merge)
          if [ -d "scripts/rterm-icons" ]; then
            cp -f scripts/rterm-icons/icon.png apps/electron/materials/icon.png
            cp -f scripts/rterm-icons/icon.icns apps/electron/materials/icon.icns
            cp -f scripts/rterm-icons/icon.ico apps/electron/materials/icon.ico
            for size in 16x16 32x32 48x48 64x64 128x128 256x256 512x512; do
              cp -f scripts/rterm-icons/${size}.png apps/electron/materials/icons/${size}.png 2>/dev/null || true
            done
            cp -f scripts/rterm-icons/mobile-web-icon.png apps/electron/mobile-web-runtime/icon.png 2>/dev/null || true
            cp -f scripts/rterm-icons/mobile-web-public-icon.png apps/mobile-web/public/icon.png 2>/dev/null || true
            echo "Restored RTerm icons from scripts/rterm-icons/"
          fi
          jq --arg v "${{ steps.check.outputs.new_version }}" '.version = $v' version.json > version.json.tmp && mv version.json.tmp version.json
          jq --arg v "${{ steps.check.outputs.new_version }}" '.version = $v' package.json > package.json.tmp && mv package.json.tmp package.json
          jq --arg v "${{ steps.check.outputs.new_version }}" '.version = $v | .name = "rterm" | .packages[""].version = $v | .packages[""].name = "rterm"' package-lock.json > package-lock.json.tmp && mv package-lock.json.tmp package-lock.json
          git add -A
          git diff --cached --stat
          git commit -m "chore: sync from GyShell v${{ steps.check.outputs.new_version }} and rebrand" --allow-empty

      - name: Push changes
        if: steps.check.outputs.should_build == 'true'
        run: git push origin main

  build-mac:
    name: Build macOS ARM64
    needs: sync
    if: needs.sync.outputs.should_build == 'true'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
      - run: npm run dist:mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
      - name: Upload artifacts
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          find dist -maxdepth 1 -name "*.dmg" | while read f; do
            echo "Uploading $f"
          done
      - uses: actions/upload-artifact@v4
        with:
          name: rterm-mac
          path: dist/*arm64.dmg
          retention-days: 1

  build-win:
    name: Build Windows x64
    needs: sync
    if: needs.sync.outputs.should_build == 'true'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
        continue-on-error: true
      - name: Remove canvas (fails without dev headers)
        shell: pwsh
        run: Remove-Item -Recurse -Force node_modules\canvas -ErrorAction SilentlyContinue
      - name: Fix Windows spawnSync
        shell: pwsh
        run: |
          $files = @('apps/electron/prepare-cli-runtime.mjs', 'apps/electron/prepare-mobile-web.mjs')
          foreach ($f in $files) {
            if (Test-Path $f) {
              $content = Get-Content $f -Raw
              $patched = $content -replace "stdio: 'inherit'", "shell: true, stdio: 'inherit'"
              Set-Content $f $patched -NoNewline
              Write-Host "Patched $f"
            }
          }
      - run: npm run dist:win
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
      - uses: actions/upload-artifact@v4
        with:
          name: rterm-win
          path: |
            dist/*Portable*.exe
            dist/*Setup*.exe
          retention-days: 1

  build-linux-x64:
    name: Build Linux x64
    needs: sync
    if: needs.sync.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: System deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends rpm fakeroot dpkg libarchive-tools
      - run: npm install || true
      - run: rm -rf node_modules/canvas
      - run: npm run dist:linux
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
      - uses: actions/upload-artifact@v4
        with:
          name: rterm-linux-x64
          path: |
            dist/*x64.AppImage
            dist/*x64.deb
            dist/*x64.pacman
            dist/*x64.rpm
          retention-days: 1

  build-linux-arm64:
    name: Build Linux ARM64
    needs: sync
    if: needs.sync.outputs.should_build == 'true'
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: System deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends rpm fakeroot dpkg libarchive-tools
      - run: npm install || true
      - run: rm -rf node_modules/canvas
      - run: npm run dist:linux-arm64
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
      - uses: actions/upload-artifact@v4
        with:
          name: rterm-linux-arm64
          path: |
            dist/*arm64.AppImage
            dist/*arm64.deb
            dist/*arm64.rpm
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: [sync, build-mac, build-win, build-linux-x64, build-linux-arm64]
    if: needs.sync.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Get version
        id: version
        run: |
          VERSION=$(jq -r '.version' version.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=v${VERSION}" >> $GITHUB_OUTPUT
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: List assets
        run: ls -lh dist/ || true
      - name: Create or update GitHub release
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_NAME: "RTerm v${{ steps.version.outputs.version }}"
          RELEASE_BODY: |
            RTerm v${{ steps.version.outputs.version }} — AI-Native Terminal App by Hyperspace Technologies

            Synced from upstream GyShell, with RTerm-specific enhancements.

            ## What's new in RTerm v1.7.4

            A visual polish + new app icon. Same engine, much better feel.

            - **New app icon** across macOS / Windows / Linux (and the CI rebrand-restore now keeps the new icon set).
            - **Command palette (⌘/Ctrl+K)** — fuzzy "do anything" launcher: open any saved SSH/WinRM/Serial connection, open the Connections/Settings panels, toggle session logging, or trigger PuTTY import, all from one keyboard-first overlay.
            - **Toast notifications** — consistent bottom-right toasts for save/delete/import with optional undo, replacing silent inline messages.
            - **Cohesive motion language** — a shared easing + duration token set, boot fade-in, and hover transitions across tabs/rows/cards.
            - **Ambient depth** — a faint theme-tinted radial background behind the app for depth without distraction.
            - **Themed overlay scrollbars** — thin, auto-hiding, themed everywhere.
            - **Connection tab status pills + type tints** — colored left-border stripe per connection type (SSH/WinRM/Serial/Local/exited).
            - **Split-pane visible grab handles** on hover.
            - **Focus-visible rings** across all interactives (accessibility).
            - **Reduced-motion support** — `prefers-reduced-motion` disables animations.
            - **CSS scaffolding** for exit-code badges, fleet results tables, device-memory cards, empty-state illustrations, and approval modals (ready to wire into components).

            ## What's new in RTerm v1.7.3

            Closes every usability gap from 1.7.2: all automation features are now fully usable via the agent AND the Connections panel, plus improved chat rendering.

            - **Every feature is now agent-usable.** `manage_ssh_connection` / `manage_winrm_connection` now accept `groupId` + `notes` (create a connection *and* put it in a folder in one call). New `manage_serial_connection` tool for serial console connections. New `list_session_logs` + `read_session_log` tools to review recorded sessions. `open_terminal_tab` and `probe_connectivity` now resolve Serial saved connections too.
            - **Every feature is now UI-usable.** The Connections panel gains new sections: **Serial** (path/baud/parity/flow), **Groups** (folder tree with parent selector), **Scripts**, **Scheduled Tasks**, and **Templates** (Jinja-subset body editor). Each SSH/WinRM/Serial editor gains a **Group** selector and a **Notes** textarea. A **PuTTY import** button parses a `.reg` file into SSH connections. The terminal **+** menu lists serial connections alongside SSH/WinRM.
            - **Improved chat rendering.** Raw HTML now renders inline (rehype-raw), images render with lazy loading, fenced code blocks carry language classes (enabling future syntax highlighting), and ` ```mermaid ` blocks render as a styled source preview with a Mermaid badge.
            - Runtime: `AutomationManager` + `SessionLogService` wired into both runtimes; the session logger is exposed to the agent for list/read; serial ConnectionManager CRUD added.

            ## What's new in RTerm v1.7.2

            Netcatty + NetStacks parity — a full local-only automation & knowledge layer, plus serial console and PuTTY import. No server required.

            - **Connection groups / folders.** Organize saved SSH/WinRM connections into a tree (parent/child); children of a deleted group reparent to root. New `manage_group` agent tool + `groupId` on connection entries.
            - **Per-device memory.** Record role, standing instructions, and dated incident history per host (with ticket ids). Standing instructions are injected into the agent's context for that host on future turns. `manage_device_memory` agent tool (get/upsert/add_incident/list/delete).
            - **Saved scripts / snippets.** A reusable command library. `manage_script` tool (create/update/delete/list); run a saved script's body on open tabs via `run_fleet_command`/`exec_command`.
            - **Scheduled tasks (cron).** Create cron-scheduled tasks that run a saved script or inline command on a target scope. A local per-minute scheduler evaluates due tasks. `manage_scheduled_task` tool (5-field cron, retry, enable/disable).
            - **Config templates (Jinja-subset).** Parameterized network config templates with `{{ var | default/upper/lower/length }}`, `{% for %}`, `{% if %}/{% elif %}/{% else %}`. `manage_template` tool: create → render (preview) → version (saves a versioned render + returns a diff vs the previous version for review/rollback). Dependency-free renderer.
            - **Session logging.** Record terminal output per session to disk (Netcatty "connection logs"). New `SessionLogService` wired into the terminal data path; an index lists recorded sessions with start/end/bytes. Enable via `sessionLogging.enabled`.
            - **Serial console connections.** New `serial` terminal type (`/dev/ttyUSB0`, COM3, baud/parity/flow-control) presented as a live PTY tab. Lazy-loads the `serialport` npm module (clear error if not installed); backend logic unit-tested with a mock transport (no serial hardware needed).
            - **PuTTY import.** `import_putty` agent tool parses a Windows .reg PuTTY session export into saved SSH connections (ssh-protocol only, duplicate names skipped).
            - **Per-connection `notes` + `groupId` fields** on SSH and WinRM entries.

            ## What's new in RTerm v1.7.1

            - **WinRM (Windows Remote Management) connectivity.** Connect to Windows servers over WS-Management on port 5985 (HTTP) / 5986 (HTTPS) with Basic auth, alongside SSH. Add WinRM connections from the Connections panel (new **WinRM** section) or with the new `manage_winrm_connection` agent tool, open a tab, and run PowerShell/cmd commands via `exec_command` / `run_fleet_command` / `collect_facts`.
            - **WinRM is scoped to command execution + the fleet tools**, rendered as a **command/response log** (not a full interactive PTY) — the right model for WinRM's request/response shell. No streaming stdin / interactive TUI (vim/top/Ctrl+C) over WinRM; everything else (guardrails, structured output, exit codes, fleet fan-out, fact inventory, connectivity probe) works on WinRM tabs just like SSH/Linux tabs.
            - **`manage_winrm_connection` agent tool** — create / update / delete / list saved WinRM connections, the Windows counterpart of `manage_ssh_connection`. `open_terminal_tab` and `probe_connectivity` now resolve WinRM saved connections too; saved WinRM connections are surfaced to the agent in system info.
            - **Dependency-free WS-Management transport** — the new `WinRMTransport` hand-implements the SOAP/WS-Man command cycle (Create shell → Command → Receive-with-CommandId → Delete) over Node's built-in http/https, with no new npm dependency. Verified end-to-end against a live Windows Server 2022 target.
            - The agent's command path (`TerminalService.runCommandAndWait`) now routes through an optional backend `executeCommand` hook for non-PTY backends, so WinRM commands bypass the SSH-style marker tracking cleanly.

            ## What's new in RTerm v1.7.0

            RTerm 1.7.0 turns the agent into a true data-center operator. Four new agent tools compose with the existing terminal/file/guardrail tools so you can provision, connect, inventory, and command whole fleets from a single chat — across Linux servers, Windows servers, and Cisco/legacy network gear.

            - **`manage_ssh_connection`** — Create, update, delete, or list saved SSH connections the same way the Connections panel does. The agent can now provision a server end-to-end: "set up my core switch at 10.0.0.1, user admin, Cisco preset, vt100" → it creates the connection, then opens a tab for it. Mutations persist to backend settings and the Connections panel refreshes live; duplicate names are rejected so `open_terminal_tab` name-matching stays unambiguous.
            - **`run_fleet_command`** — Run the same command on many open tabs at once and get one structured, machine-parseable `<fleet_results>` block (per-target OK/FAIL, exit code, output). "show version on all core switches", "free -m on every web node", "systemctl status nginx across the farm". The command is policy-checked once for the whole fleet, then fanned out in parallel; duplicates and not-ready tabs are handled gracefully.
            - **`collect_facts`** — Inventory one or more open tabs (or all of them) with a small per-OS fact template (hostname, OS/version, uptime, interfaces) and return a structured `<inventory>` JSON block. A `defaultClass` hint (network/linux/windows) classifies raw-shell Cisco tabs that report no OS.
            - **`probe_connectivity`** — Probe a saved connection's reachability: open a fresh tab (or reuse an open one), wait for ready/exit, and report REACHABLE/UNREACHABLE plus the detected OS class, status header, and login banner. The building block for autonomous ops and pre-change sanity checks.

            All four go through the existing command-policy guardrails (allow/deny/ask + approval) and the shell-integration command tracking, so fleet ops are as safe as single-tab ops. Saved connections surfaced to the agent in system info now drive the whole lifecycle.

            ## What's new in RTerm v1.6.3

            - **Fix: Algorithms preset + TERM actually reach the SSH backend.** In v1.6.1/v1.6.2, connecting from the Connections panel dropped the *Algorithms* and *TERM* fields when building the runtime config, so Cisco/legacy raw-shell mode never engaged (SFTP warning + hang at "Opening interactive shell..." persisted). The saved connection's `algorithmsPreset` and `termType` are now forwarded, so **Algorithms: Cisco / Network gear + TERM: vt100** works as intended.

            ## What's new in RTerm v1.6.2

            - **Fix: Cisco / legacy SSH interactive shell now opens.** On v1.6.1 the connection succeeded but the shell hung at "Opening interactive shell..." because ssh2 sent `env` channel requests that Cisco IOS doesn't acknowledge, and the POSIX shell-integration injection (`base64`/`eval`) produced garbage on a network OS. The `cisco` and `legacy` presets now use a **raw-shell mode**: no `env` requests, no SFTP init, no OS-detection exec probes, and no shell-integration injection — the device's prompt streams straight through. Set **Algorithms: Cisco / Network gear** (or **Legacy**) and **TERM: vt100** in the Connections panel.

            ## What's new in RTerm v1.6.1

            - **Legacy / Cisco SSH support.** New *Algorithms* preset (Modern / Legacy / Cisco) and optional *TERM* type field on saved SSH connections. Older network gear (Cisco IOS/IOS-XE, legacy switches/routers) that only offers `diffie-hellman-group1-sha1`, `ssh-rsa`, `aes*-cbc`, `hmac-sha1` now connects successfully instead of failing the SSH handshake. Select **Algorithms: Cisco / Network gear** (or **Legacy**) in the Connections panel.
            - **`open_terminal_tab` agent tool.** The AI agent can now open a new SSH terminal tab from a saved connection by Name or ID, instead of only operating on already-open tabs. Saved SSH connections are surfaced to the agent in the system info. Duplicate-open is detected and reported. Enables fully autonomous "connect to server X and run …" workflows.

            ## Downloads

            | Platform | File |
            |----------|------|
            | macOS ARM64 | `RTerm-${{ steps.version.outputs.version }}-arm64.dmg` |
            | Windows x64 (Portable) | `RTerm.Portable.${{ steps.version.outputs.version }}-x64.exe` |
            | Windows x64 (Setup) | `RTerm.Setup.${{ steps.version.outputs.version }}-x64.exe` |
            | Linux x64 (AppImage) | `RTerm-${{ steps.version.outputs.version }}-x64.AppImage` |
            | Linux x64 (deb) | `RTerm-${{ steps.version.outputs.version }}-x64.deb` |
            | Linux x64 (pacman) | `RTerm-${{ steps.version.outputs.version }}-x64.pacman` |
            | Linux x64 (rpm) | `RTerm-${{ steps.version.outputs.version }}-x64.rpm` |
            | Linux ARM64 (AppImage) | `RTerm-${{ steps.version.outputs.version }}-arm64.AppImage` |
            | Linux ARM64 (deb) | `RTerm-${{ steps.version.outputs.version }}-arm64.deb` |
            | Linux ARM64 (rpm) | `RTerm-${{ steps.version.outputs.version }}-arm64.rpm` |

            ## Install

            **macOS:** After installing, run `xattr -cr /Applications/RTerm.app` to remove quarantine.

            **Linux AppImage:** `chmod +x RTerm-*.AppImage && ./RTerm-*.AppImage`

            **Linux deb:** `sudo dpkg -i RTerm-*.deb`

            **Linux rpm:** `sudo rpm -i RTerm-*.rpm`

            See [GyShell changelog](https://github.com/MrOrangeJJ/GyShell/releases/tag/v${{ steps.version.outputs.version }}) for details.

            ---

            Contact: rterm@hyperspace.ng
        run: |
          set -uo pipefail
          printf '%s\n' "$RELEASE_BODY" > /tmp/release-notes.md
          ASSETS="dist/*.dmg dist/*.exe dist/*.AppImage dist/*.deb dist/*.pacman dist/*.rpm"
          # Resilient create/update: retries through transient GitHub API 5xx
          # (softprops/action-gh-release failed repeatedly with 503 Unicorn
          # pages on getReleaseByTag). gh CLI + explicit retry avoids that.
          OK=0
          for attempt in 1 2 3 4 5; do
            if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
              # Release already exists (e.g. a prior attempt published it
              # without assets) — upload/overwrite assets.
              if gh release upload "$RELEASE_TAG" --clobber $ASSETS; then
                echo "Assets uploaded to existing release $RELEASE_TAG"
                OK=1
                break
              fi
            else
              if gh release create "$RELEASE_TAG" \
                  --title "$RELEASE_NAME" \
                  --notes-file /tmp/release-notes.md \
                  --target main \
                  $ASSETS; then
                echo "Created release $RELEASE_TAG with assets"
                OK=1
                break
              fi
            fi
            echo "Release attempt $attempt failed; retrying in 20s..."
            sleep 20
          done
          if [ "$OK" != "1" ]; then
            echo "::error::Failed to create/upload release $RELEASE_TAG after 5 attempts"
            exit 1
          fi
          gh release view "$RELEASE_TAG" --json name,tagName,assets | jq '{tag:.tagName, name, assets:[.assets[].name]}'

      - name: Notify Multica autopilot
        if: success()
        run: |
          # Fire the Multica RTerm Release Pipeline autopilot directly.
          # We cannot rely on the release: published event because GitHub
          # blocks that event from triggering downstream workflows when the
          # release is created inside a running workflow (even with a PAT).
          python3 -c "
          import json, urllib.request, os
          payload = json.dumps({
            'action': 'published',
            'release': {
              'tag_name': 'v${{ steps.version.outputs.version }}',
              'draft': False,
              'prerelease': False
            },
            'repository': {
              'full_name': 'DrOlu/RTerm',
              'name': 'RTerm'
            }
          }).encode()
          req = urllib.request.Request(
            'https://api.multica.ai/api/webhooks/autopilots/awt_jZXas3khd6pPhcNi7CkZTSBRbI_iDUsz-jSV2PN_blw',
            data=payload,
            headers={'Content-Type': 'application/json'}
          )
          urllib.request.urlopen(req)
          print('Multica RTerm autopilot notified')
          "

      - name: Notify via Telegram
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          STATUS="✅ Released"
          if [ "${{ job.status }}" != "success" ]; then STATUS="❌ Failed"; fi
          VERSION="${{ steps.version.outputs.version }}"
          PAYLOAD=$(jq -nc \
            --arg chat_id "$TELEGRAM_CHAT_ID" \
            --arg text "${STATUS} *RTerm v${VERSION}*

          Built from GyShell v${VERSION}.
          All platforms: macOS, Windows, Linux (x64 + ARM64).

          https://github.com/DrOlu/RTerm/releases/tag/v${VERSION}" \
            '{chat_id:$chat_id,parse_mode:"Markdown",text:$text}')
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" || true
